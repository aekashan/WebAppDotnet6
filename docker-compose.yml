version: '3'

services:
    webapp:
        image: ekachansr/webappdotnet6:latest
        ports:
          - "5000:80"
        container_name: webappdotnet6
        volumes:
          - ./logs:/app/logs